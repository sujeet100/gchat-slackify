# Pin the PUBLIC npm registry for this project so CI (and contributors) never inherit a
# private/global registry from a user-level ~/.npmrc. This file overrides ~/.npmrc here, which
# is what keeps package-lock.json on npmjs.org URLs (no auth needed in CI).
registry=https://registry.npmjs.org/
